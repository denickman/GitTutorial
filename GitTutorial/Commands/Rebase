
// Rebase

git checkout -b feature
make changes in feature

git checkout master
make changes in master

git checkout feature
git rebase master

git difftool // resolve conflicts

git add name_of_changed_file

git rebase --continue

git commit -am "Adding changes after rebasing conflicts"

git checkout master

git merge feature


REBASE
git rebase master // currently on newFeature branch
git rebase --abort // restore without rebase branches/initial state
git rebase --continue // continue after resolve conflicts
git merge bigtrouble // on master branch and merge after rebasing

git fetch origin master
git pull --rebase origin master



